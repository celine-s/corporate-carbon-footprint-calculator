export type Question = {
  id: string;
  title: string;
  category: string;
  label?: string;
  initialAnswer?: string;
  emissionfactor?: string;
  infobox?: string;
  whatTitle?: string;
  whatText?: string;
  constraints?: string;
};
